# ~/.config/mm/mm.toml
#
# mm configuration file.
# Copy to ~/.config/mm/mm.toml and edit to your needs.
# Generate with: mm config init

# ── Active profile ──────────────────────────────────────────────────
# Switch with:  mm config profile use <name>
# Per-command:  mm --profile vlmrun cat photo.png -l 2
# Env var:      MM_PROFILE=vlmrun mm cat photo.png -l 2
active_profile = "default"

# ── Profiles (LLM provider configs) ────────────────────────────────
# Each profile has: base_url, api_key, model
# List all:     mm config profile list
# Add new:      mm config profile add <name> --base-url <url> --model <model>
# Update:       mm config profile update <name> --api-key <key>

[profile.default]
# Ollama (local, default)
base_url = "http://localhost:11434"
api_key = ""
model = "qwen3.5:0.8b"

# [profile.vllm]
# base_url = "http://localhost:8000/v1"
# api_key = ""
# model = "Qwen/Qwen3.5-0.8B"

# [profile.vlmrun]
# base_url = "https://api.vlm.run/v1"
# api_key = ""
# model = "vlm-1"

# [profile.openrouter]
# base_url = "https://openrouter.ai/api/v1"
# api_key = ""
# model = "qwen/qwen-2.5-vl-7b-instruct"

# [profile.openai]
# base_url = "https://api.openai.com/v1"
# api_key = ""
# model = "gpt-4o"

# [profile.gemini]
# base_url = "https://generativelanguage.googleapis.com/v1beta/openai/"
# api_key = ""
# model = "gemini-2.5-flash"


# ── Environment variable overrides ───────────────────────────────────
#
# These override config file values:
#
#   MM_BASE_URL    — override base_url from active profile
#   MM_API_KEY     — override api_key from active profile
#   MM_MODEL       — override model from active profile
#   MM_PROFILE     — override active_profile
#
# Resolution order:
#   env vars  >  active profile  >  built-in defaults
