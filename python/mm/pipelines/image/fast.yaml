kind: image
mode: fast

encode:
  strategy: resize
  strategy_opts:
    max_width: 512

generate:
  prompt: >-
    Describe this image in 10 words or less.
    Then list exactly 5 keyword tags.

    [OUTPUT]
    {{description}}

    Tags: {{up to 5 keyword tags}}
  max_tokens: 256
