kind: code
mode: accurate

encode:
  strategy: null

generate:
  prompt: >-
    Summarize this code file ({filename}) in about 80 words.
    Describe its purpose, key functions, and dependencies.

    ```
    {content}
    ```
  max_tokens: 512
