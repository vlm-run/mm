kind: document
mode: accurate

encode:
  strategy: page-text
  pages_per_message: 4

generate:
  prompt: >-
    Structure this document ({filename}) into a clean markdown readable format.
    The conversion should be lossless and well-organized/formatted with proper headings, lists, etc.

    ```text
    {content}
    ```
